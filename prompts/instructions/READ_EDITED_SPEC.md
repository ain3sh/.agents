---

[Edited Spec — re-read before implementing]

The user edited the spec on disk before approving. Read `${filePath}` in full now; the on-disk file is source of truth, not the chat-displayed plan. Reconcile **files to modify**, **key decisions**, and **risks** against your memory, then re-anchor todos against the edited version. Any drift collapses in favor of the file.

---
